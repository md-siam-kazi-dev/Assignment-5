1️⃣ What is the difference between var, let, and const?
ans:if we chose declare a variable using const ,later we can not change the 
  variable value .If we commited to change value it throw error.It is oposite
  declare variable using let , we can change variable value.var is function or global scope .Let and const is loacl scope
2️⃣ What is the spread operator (...)?
ans:spread operator use to referance an array copy.
3️⃣ What is the difference between map(), filter(), and forEach()?
ans:map => it operate function every value and return array.
    filter => It filter value that matche the condition in the function.
    forEach => It occour operation every value and doesn't return any array.
4️⃣ What is an arrow function?
ans:arraw function is shorter js function where if we write one line code it doesn't need return statement.
5️⃣ What are template literals?
ans:Template literals is dynamic string where we can change value using ${var}.
